import 'package:flutter/widgets.dart';

Widget buildYouTubeEmbed(String videoId) => const SizedBox.shrink();

Widget buildNetworkVideo(String url) => const SizedBox.shrink();

Widget buildDocumentEmbed(String url) => const SizedBox.shrink();
